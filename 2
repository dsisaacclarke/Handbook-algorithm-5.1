#include <iostream>

int main() {

    int a = 0, b = 1, c, n;

    std::cin >> n;

    if (n <= 1) {
        std::cout << n << "\n";
        return 0;
    }

    for (int i = 2; i <= n; ++i) {
        c = (a + b) % 10;
        a = b;
        b = c;
    }
    
    std::cout << b;
}
